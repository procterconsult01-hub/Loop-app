PORT=3001
JWT_SECRET=replace-with-a-long-random-string

# Local Postgres example — on Railway, this is injected automatically once you
# attach a Postgres add-on, so you won't need to set it yourself there.
DATABASE_URL=postgres://postgres:password@localhost:5432/loop
